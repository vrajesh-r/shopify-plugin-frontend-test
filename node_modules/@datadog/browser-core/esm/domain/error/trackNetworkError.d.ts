import { RawError } from '../../tools/error';
import { Observable } from '../../tools/observable';
import { Configuration } from '../configuration';
export declare function trackNetworkError(configuration: Configuration, errorObservable: Observable<RawError>, trackAbortedRequests?: boolean): {
    stop: () => void;
};
