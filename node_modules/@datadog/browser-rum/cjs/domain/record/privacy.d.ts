import { InputPrivacyMode } from '../../constants';
export declare function nodeShouldBeHidden(node: Node): boolean;
export declare function nodeOrAncestorsShouldBeHidden(node: Node | null): boolean;
/**
 * Returns the given node input privacy mode. The ancestor input privacy mode is required to make
 * sure we respect the privacy mode priorities.
 */
export declare function getNodeInputPrivacyMode(node: Node, ancestorInputPrivacyMode: InputPrivacyMode): InputPrivacyMode;
/**
 * Returns the given node input privacy mode. This function is costly because it checks all of the
 * node ancestors.
 */
export declare function getNodeOrAncestorsInputPrivacyMode(node: Node): InputPrivacyMode;
